Viridian Systems Intelligence Demo

Local heuristic demo scaffold.

Install and build

```bash
cd demo
npm install
npm run build
# Then open the main site root (dev server or open public/index.html). The demo build will output to ../public/demo
```

Dev mode (serve the demo separately)

```bash
cd demo
npm install
npm run dev
# Visit http://localhost:5173
```
